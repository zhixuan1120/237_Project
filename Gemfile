source 'https://rubygems.org'
ruby '2.4.0'


# ~> "equal to or greater than in the last digit", 
# e.g. ~> 2.3 means "equal to 2.3 or greater than 2.3, but less than 3.0"

gem 'rails', '5.0.1'
gem 'bootstrap-sass'
gem 'sass-rails'
gem 'bcrypt-ruby'
gem 'will_paginate'
gem 'bootstrap-will_paginate'
gem 'warden'
gem 'redis'
gem 'private_pub'
gem 'thin'
gem 'devise'
gem 'rack'
gem 'railties'
gem 'sass'
gem 'sprockets'
gem 'sprockets-rails'
gem 'rails_12factor'
gem 'uglifier'
gem 'jquery-rails'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

group :development do
  gem 'pg'
end

group :test do
  gem 'pg'
  gem 'capybara', '1.1.2'
end

group :production do
  gem 'pg'
  gem 'foreman'
  gem 'puma'
end

# To use Jbuilder templates for JSON
# gem 'jbuilder'

#Use unicorn as the app server
#gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger# gem 'debugger'