<% title("Welcome to 一pie即合") %>
<%= javascript_include_tag "users.js" %>
<div class="row">
	<!-- <aside class="col-xs-6">
		<p> 在你生命的某一刻，是否也遇到那个让你心动的TA？</p>
		<p>	在你感情的某一段，是否也有对TA无法开口的歉意？</p>
		<p>	大胆说出你的心声，不要让生活里留下遗憾与泪水。</p>
	</aside> -->
	
	<!-- <div class="col-xs-4 col-xs-offset-4" style="margin-top: 225px;"> -->
  		<div id="placeholder" style="width: 330px; margin: 0 auto;"></div>
  		<%= form_for(@user, url: users_path, method: 'post', remote: true) do |f| %>
      		<%= render 'shared/error_messages', object: f.object %> 

			<%= f.text_field :name, placeholder: "Name", class: "form-control", autocomplete: "off" %>

			<%= f.text_field :email, placeholder: "Email", class: "form-control", autocomplete: "off" %>
			
			<%= f.password_field :password, placeholder: "Password", class: "form-control", autocomplete: "off" %>

			<%= f.password_field :password_confirmation, placeholder: "Confirm Password", class: "form-control", autocomplete: "off" %>
  	  		
  	  		<%= f.submit "Join Now", id: "join", class: "btn btn-primary", data: { disable_with: "Please wait..." } %>
  		<% end %>	
	<!-- </div> -->
	
</div>